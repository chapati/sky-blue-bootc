name: docker
description: Disable rootful docker

modules:
  - type: service
    mask:
      - name: docker.socket
        scope: system
      - name: docker.service
        scope: system
  - type: script
    snippets:
      - "rm /var/run/docker.sock || true"
