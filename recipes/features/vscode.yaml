name: vscode
description: vscode config

modules:
  - type: script
    snippets: |
      rpm --import /build/files/keys/microsoft-vscode.asc

  - type: dnf
    repos:
      - vscode.repo
    install:
      - code
