# TODO: check hardware acceleration
# TODO: check error messages in log
name: Google Chrome
description: Install google chrome stable

modules:
  - type: dnf
    repos:
      - google-chrome.repo
    install:
      - google-chrome-stable